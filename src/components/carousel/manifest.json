{
  "$schema": "../../../schema/manifest.schema.json",
  "name": "carousel",
  "kind": "component",
  "description": "Slides on a scroll-snapping track with dots that link to each one, scrolled by the browser and needing no script.",
  "class": "carousel",
  "attributes": [
    { "name": "data-slides", "type": "enum", "vocabulary": "slides", "default": "1", "description": "How many slides are visible at once." },
    { "name": "data-gap", "type": "enum", "vocabulary": "gap", "default": "md", "description": "Space between slides, and above the dots." }
  ],
  "classes": [],
  "children": [
    { "selector": "> [data-track]", "min": 1, "max": 1, "description": "The scrolling track holding the slides. A scrolling region is an interactive one, so give it role=\"group\", a name, and tabindex=\"0\"." },
    { "selector": "[data-slide]", "min": 2, "max": null, "description": "One slide each, with an id a dot can link to." },
    { "selector": "> [data-dots]", "min": 0, "max": 1, "description": "A list of links, one per slide, each named with aria-label." }
  ],
  "tokens": [
    { "name": "--yeti-carousel-dot", "public": true, "description": "Colour of a dot." },
    { "name": "--yeti-carousel-dot-size", "public": true, "description": "Diameter of a dot." },
    { "name": "--yeti-control-size", "public": true, "description": "The pressable area around a dot." },
    { "name": "--_yeti-slides", "public": false },
    { "name": "--_yeti-gap", "public": false }
  ],
  "a11y": {
    "requiredAttributes": ["aria-label"],
    "keyboard": [
      { "key": "Tab", "action": "Focuses the track, then each dot in turn." },
      { "key": "ArrowLeft / ArrowRight", "action": "Scroll the track." },
      { "key": "Enter", "action": "Scrolls to that dot's slide." }
    ],
    "notes": "Label the region and give it aria-roledescription=\"carousel\" so it is announced as one. The dots are links to slide ids, so the browser does the scrolling and the keyboard works without help; each needs an aria-label, since a dot has no room for text and Yeti ships no visually-hidden utility. They jump to a slide but cannot report which slide is showing, because CSS cannot know that. Nothing essential should live behind a slide a reader has to find. The track scrolls, so it must be reachable from the keyboard: give it role=\"group\", an aria-label, and tabindex=\"0\". It is a group rather than a region because the carousel itself is already the landmark."
  },
  "js": null,
  "support": { "unguarded": ["scroll snap", "scroll-behavior"], "guarded": [] },
  "since": "7.0.0",
  "example": "example.html"
}
