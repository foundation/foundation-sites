{
  "$schema": "../../../schema/manifest.schema.json",
  "name": "breadcrumbs",
  "kind": "component",
  "description": "The trail from the home page to the current one, each step a link, with a separator between them that is seen and not read.",
  "class": "breadcrumbs",
  "attributes": [
    { "name": "data-size", "type": "enum", "vocabulary": "size-control", "default": "md", "description": "The text step." }
  ],
  "classes": [],
  "children": [
    { "selector": "> ol", "min": 1, "max": 1, "description": "The trail, in order, with role=\"list\"." },
    { "selector": "li", "min": 2, "max": null, "description": "A step: a link, or plain text with aria-current=\"page\" for the last." }
  ],
  "tokens": [
    { "name": "--yeti-breadcrumbs-separator", "public": true, "description": "The string between steps." },
    { "name": "--yeti-color-text-muted", "public": true, "description": "The links and the separator." },
    { "name": "--_yeti-size-text", "public": false }
  ],
  "a11y": { "requiredAttributes": ["aria-label"], "keyboard": [], "notes": "Put aria-label=\"Breadcrumb\" on the nav. The last step carries aria-current=\"page\", as plain text or as a link to the page itself. The separators are CSS with empty alternative text, so assistive tech reads only the steps. Put role=\"list\" on the ol." },
  "js": null,
  "support": { "unguarded": ["alternative text on generated content"], "guarded": [] },
  "since": "7.0.0",
  "example": "example.html"
}
